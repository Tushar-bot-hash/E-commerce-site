{
  "version": 2,
  "buildCommand": "cd Frontend-app && npm install && npm run build",
  "outputDirectory": "Frontend-app/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}